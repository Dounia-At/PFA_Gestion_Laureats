@model List<Entreprise>
@section title {
   Entreprises
}
@section Style{
    <style>
       
    </style>
}
@section js {


    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.css" />


    <script src="https://cdnjs.cloudflare.com/ajax/libs/sweetalert/1.1.3/sweetalert.min.js"></script>

    <!-- JavaScript -->
    <script>

        var deleteButtons = document.querySelectorAll(".deleteButton");
        deleteButtons.forEach(function (button) {
            button.addEventListener("click", function (event) {

                event.preventDefault();


                swal({
                    title: "Êtes-vous sûr de vouloir supprimer cette Entreprise ?!",
                    text: "Cette action ne peut pas être annulée !",
                    type: "warning",
                    showCancelButton: true,
                    confirmButtonColor: "#DD6B55",
                    confirmButtonText: "Oui, supprimer !",
                    cancelButtonText: "Annuler",
                    closeOnConfirm: false,
                    closeOnCancel: true
                },
                    function (isConfirmed) {
                        if (isConfirmed) {

                            var form = document.createElement("form");
                            form.method = "post";
                            form.action = "@Url.Action("Delete", "Entreprise")/" + button.getAttribute("data-id");
                            form.innerHTML = '@Html.AntiForgeryToken()';
                            var submitButton = document.createElement("button");
                            submitButton.type = "submit";
                            submitButton.style.display = "none";
                            form.appendChild(submitButton);
                            document.body.appendChild(form);
                            form.submit();
                        }
                    });
            });
        });
    </script>
}
<h3 class="fw-bold py-2 mb-2 card-header ">
    <a class="text-muted fw-light" asp-controller="Annonce" asp-action="Annonces">Accueil /</a>
    Entreprises
</h3>

@if (ViewBag.role == "AgentDirection")
{
    <div class="container mt-5 d-flex flex-row-reverse">
        <a class="btn btn-primary" asp-controller="Entreprise" asp-action="Add">
            <i class="ti ti-plus me-1"></i>
            <span class="align-middle">Nouvelle Entreprise</span>
        </a>
        <br />
    </div>
    
}
<div class="container-fluid my-4">
    @if (Model != null)
    {
        <div class="row g-4">
            @foreach (Entreprise entreprise in Model)
            {
                 <div class="col-xl-4 col-lg-6 col-md-6">
                        <div class="card" style="max-height:100% !important">
                            <div class="card-body">
                                <div class="d-flex align-items-center mb-3">
                                    <a asp-controller="Entreprise" asp-action="Details" asp-route-id="@entreprise.Id" class="d-flex align-items-center">
                                        <div class="avatar me-2">
                                            <img src="../../assets/img/logo/@entreprise.Logo"
                                                 alt="Logo"
                                                 class="rounded-circle" />
                                        </div>
                                        <div class="me-2 text-body h5 mb-0">@entreprise.Nom</div>
                                    </a>

                                </div>
                            <div class="mb-1">
                               @* <p >@entreprise.Pays @entreprise.Ville</p>*@
                               
                                <p class=" my-4"> @entreprise.Adresse.Substring(0, Math.Min(20, entreprise.Adresse.Length)) <span class="text-muted  mb-2">...</span></p>
                               

                                    <hr class="my-0" /> <br />
                                    @* <a asp-controller="Annonce" asp-action="Details" asp-route-id="@annonce.Id">
                            <p class="mx-3 my-2 text-primary">Plus Details ...</p>
                            </a>*@
                                </div>
                            <div class="d-flex align-items-center pt-1">
                                <div class="d-flex align-items-center">
                                   
                                        <span class="text-muted fw-light">@entreprise.Ville,@entreprise.Pays</span>
                                   
                                </div>
                                <div class="ms-auto">
                                    @if (ViewBag.role == "AgentDirection")
                                    {
                                        <a asp-controller="Entreprise" asp-action="Update" asp-route-id="@entreprise.Id" class="btn btn-label-success btn-icon waves-effect mx-2 ">
                                            <i class="ti ti-edit ti-sm"></i>
                                        </a>
                                        <button class="btn btn-danger btn-icon waves-effect   deleteButton" data-id="@entreprise.Id"><i class="ti ti-trash ti-sm"></i></button>



                                    }
                                </div>
                            </div>

                               

                            </div>

                        </div>
                    </div>

                
            }
        </div>
    }
    else
    {
        <div>
            <h3>Aucune entreprise pour le moment!!</h3>
        </div>
    }
</div>